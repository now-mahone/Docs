HYPERLIQUID_PRIVATE_KEY=your_hyperliquid_private_key
SOLVER_LIVE=true
RPC_URL=https://base.llamarpc.com

# =============================================================================
# WebSocket RPC (required for V2 instant event-driven rebalancing)
# Supports wss:// endpoints from Alchemy, Infura, QuickNode, Drpc, etc.
# If blank, the bot falls back to HTTP polling every 10 s.
# Example (Alchemy Base): wss://base-mainnet.g.alchemy.com/v2/<API_KEY>
# Example (Drpc free):     wss://base.drpc.org
# =============================================================================
WS_URL=wss://base.drpc.org

# =============================================================================
# Circuit Breaker Configuration (Dynamic Hedge Rebalancing Engine V2)
# These hard limits prevent over-hedging from any logic bug or RPC anomaly.
# =============================================================================
# CB1 — Max ETH notional that can be traded in a single cycle (default: 50 ETH)
CB_MAX_TRADE_ETH=50.0
# CB2 — Max allowed over-hedge as a ratio of active TVL (default: 1.05 = 5% over)
CB_MAX_OVERHHEDGE_RATIO=1.05
# CB3 — Minimum TVL sanity floor in ETH; ignore cycles below this (default: 0.1)
CB_MIN_TVL_ETH=0.1
# CB4 — Consecutive CB trips before panic/position close is triggered (default: 3)
CB_MAX_TRIPS=3
MIN_SCAN_INTERVAL=2.0
STRATEGIST_PRIVATE_KEY=your_strategist_private_key
PRIVATE_KEY=your_private_key
VAULT_ADDRESS=0x...
FLASH_ARB_BOT_ADDRESS=0x...
INSURANCE_FUND_ADDRESS=0x...
TREASURY_ADDRESS=0x...
KERNE_TOKEN_ADDRESS=0x...
KERNE_STAKING_ADDRESS=0x...
PSM_ADDRESS=0x...
KUSD_OFT_V2_ADDRESS=0x...
KERNE_OFT_V2_ADDRESS=0x...
DISCORD_WEBHOOK_URL=your_discord_webhook_url
BASESCAN_API_KEY=your_basescan_api_key
EXCHANGE_DEPOSIT_ADDRESS=0x...
# =============================================================================
# ZIN (Zero-Fee Intent Network) Configuration
# =============================================================================
# ZIN transforms Kerne into Base's primary execution engine for high-volume trading.
# It monitors CowSwap and UniswapX for profitable intents and fulfills them using
# Kerne's internal liquidity, capturing the spread as profit.

# Enable live mode (set to true to execute real transactions)
ZIN_SOLVER_LIVE=false

# Deployed ZIN contract addresses (Base Mainnet)
ZIN_EXECUTOR_ADDRESS=0x04F52F9F4dAb1ba2330841Af85dAeeB8eaC9E995
ZIN_POOL_ADDRESS=0xB9BdF6F3Fc3819b61f6fE799bE1395501822d0c7

# Profit vault - where captured spreads are deposited
PROFIT_VAULT_ADDRESS=0x...

# 1inch API key for aggregated swap quotes (required for live mode)
# Get your API key at: https://portal.1inch.dev/
ONE_INCH_API_KEY=your_1inch_api_key

# Active chain for UniswapX order fetching (base, arbitrum, mainnet, unichain)
ZIN_CHAINS=base,arbitrum
ACTIVE_CHAIN=base

# ZIN micro-cap guardrails (live mode safety limits)
ZIN_MIN_PROFIT_BPS=8
ZIN_MAX_INTENT_AMOUNT=0
ZIN_MAX_GAS_PRICE_GWEI=40
ZIN_MAX_INTENTS_PER_CYCLE=2
ZIN_MIN_ORDER_TTL_SECONDS=30
ZIN_MAX_PRICE_IMPACT_BPS=75
# Per-token caps (format: SYMBOL:amount or address:amount, comma-separated)
# Example: WETH:25000000000000000,USDC:5000000
ZIN_MAX_INTENT_AMOUNT_BY_TOKEN=

# =============================================================================
# Telemetry & Daily Reporting Configuration
# =============================================================================
# Automated daily profit reporting system that posts to Discord and saves
# markdown reports to docs/reports/

# Schedule: Hour (UTC) when daily report runs (default: 9:00 AM UTC)
TELEMETRY_HOUR_UTC=9
TELEMETRY_MINUTE_UTC=0

# Run telemetry immediately on container startup (default: true)
TELEMETRY_RUN_ON_STARTUP=true

# Override for testing: Fixed interval in hours (leave empty for daily schedule)
# Examples: "1" = hourly, "0.5" = every 30 minutes, "" = daily at scheduled time
TELEMETRY_INTERVAL_HOURS=

# =============================================================================
# Proof of Reserve (PoR) Configuration
# =============================================================================
# Automated daily solvency attestation system that:
# - Aggregates on-chain assets across Base + Arbitrum vaults
# - Verifies off-chain CEX equity (Hyperliquid)
# - Signs cryptographic attestations
# - Generates public JSON for API consumption
# - The "Glass House Standard" - proving every asset is accounted for

# Schedule: Hour (UTC) when daily PoR runs (default: 00:00 UTC / Midnight)
POR_HOUR_UTC=0
POR_MINUTE_UTC=0

# Run PoR immediately on container startup (default: true)
POR_RUN_ON_STARTUP=true

# Post attestation results to Discord (default: true)
POR_POST_DISCORD=true

# Override for testing: Fixed interval in hours (leave empty for daily schedule)
# Examples: "1" = hourly, "0.5" = every 30 minutes, "" = daily at scheduled time
POR_INTERVAL_HOURS=

# ETH price in USD for off-chain equity conversion (fallback if no oracle)
ETH_PRICE_USD=3300

# =============================================================================
# KERNE Token Buyback Flywheel Configuration
# =============================================================================
# Automated daily buyback system that:
# - Monitors Treasury WETH/USDC balances
# - Executes Aerodrome swaps to buy KERNE tokens
# - Burns purchased KERNE or sends to stakers
# - Creates persistent deflationary pressure on token supply
# - The "Liquidity Black Hole" - making KERNE the best store of value

# Buyback thresholds (minimum balances before executing buyback)
# WETH amount in wei (default: 0.1 ETH = 100000000000000000)
BUYBACK_MIN_WETH=100000000000000000
# USDC amount in 6 decimals (default: 250 USDC = 250000000)
BUYBACK_MIN_USDC=250000000

# Cooldown between buybacks in hours (default: 24 hours)
BUYBACK_COOLDOWN_HOURS=24

# Token addresses (Base Mainnet)
WETH_ADDRESS=0x4200000000000000000000000000000000000006
USDC_ADDRESS=0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913

# Aerodrome Router (Base Mainnet)
AERODROME_ROUTER=0xcF77a3Ba9A5CA399B7c97c74d54e5b1Beb874E43

# Slippage tolerance in basis points (default: 500 = 5%)
BUYBACK_SLIPPAGE_BPS=500

# =============================================================================
# Arbitrum Configuration
# =============================================================================
ARBITRUM_RPC_URL=https://arb1.arbitrum.io/rpc
ARBITRUM_ZIN_POOL_ADDRESS=0x5D8ddE6264DF8A0963253693f32e057e1aA37aFD
ARBITRUM_VAULT_ADDRESS=0x503D930dF68a68cdFeb8DEa173ADD8DD377841FF

# =============================================================================
# RPC Configuration (with fallback support)
# =============================================================================
BASE_RPC_URL=https://mainnet.base.org
# Alternative RPCs (used as fallback):
# - https://base.llamarpc.com
# - https://base.drpc.org

# =============================================================================
# Autonomous Email Outreach Configuration
# =============================================================================
# Sends institutional outreach emails to whale leads via SMTP.
# Supports Resend, SendGrid, or ProtonMail Bridge.
#
# Setup (Resend.com - Recommended Free Path):
# 1. Create a free account at https://resend.com
# 2. Verify your domain (kerne.systems) in their dashboard
# 3. Generate an API Key
# 4. Set SMTP_HOST=smtp.resend.com, SMTP_PORT=465, SMTP_USER=resend, SMTP_PASS=your_api_key
#
# Setup (ProtonMail Bridge - Paid Path):
# 1. Install ProtonMail Bridge (paid plan required)
# 2. Set SMTP_HOST=127.0.0.1, SMTP_PORT=1025, SMTP_USER=contact@kerne.systems, SMTP_PASS=bridge_password

SMTP_HOST=smtp.resend.com
SMTP_PORT=465
SMTP_USER=resend
SMTP_PASS=your_smtp_password_or_api_key
FROM_EMAIL=contact@kerne.systems
AUTONOMOUS_OUTREACH=false
